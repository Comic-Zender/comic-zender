spring:
  sql:
    init:
      platform: none
  jpa:
    show-sql: true
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5901/zender?currentSchema=comic-service
    username: zender
    password: password

  flyway:
    schemas:
      - comic-service
    baselineOnMigrate: true
    locations: classpath:db/migration
